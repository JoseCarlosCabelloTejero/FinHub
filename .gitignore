node_modules
dist
.DS_Store
coverage
*.local
*.tsbuildinfo
vite.config.js
vite.config.d.ts

# Enlace local al proyecto de Vercel (project.json con los IDs), lo crea la CLI
.vercel

# Claude Code local config y agent skills (instalación personal, no se comparte)
.claude/
.agents/
skills-lock.json

# el proyecto usa npm, no pnpm
pnpm-lock.yaml

# Estado de workspace de Obsidian sobre el vault de docs/: cambia cada vez que se abre
.obsidian/
